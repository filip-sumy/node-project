<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <title>Пости</title>
</head>
<body>
  <h1>Список постів</h1>

  <% if (posts.length === 0) { %>
    <p>Немає постів для відображення.</p>
  <% } else { %>
    <ul>
      <% posts.forEach(post => { %>
        <li>
          <h2><%= post.title %></h2>
          <p><%= post.content %></p>
          <small>Дата створення: <%= post.createdAt.toLocaleString() %></small>
        </li>
      <% }) %>
    </ul>
  <% } %>

</body>
</html>
